# Revive Hogar — Cloud Functions env (TEMPLATE — no real values)
# Copy to functions/.env.revive-hogar and fill in real values.
# functions/.env.revive-hogar is gitignored.

# Sheets integration
SHEETS_SHEET_NAME=Total consolidado

# SMTP (payment reminder emails)
SMTP_HOST=mail.smtp2go.com
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Revive Hogar <cgumucio93@gmail.com>
EMAIL_DEV_OVERRIDE=

# Payku
PAYKU_PUBLIC_TOKEN=
PAYKU_PRIVATE_TOKEN=

# Transferencias inbound (Gmail API)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# MiniMax M3 (LLM fallback for non-BICE banks)
MINIMAX_API_KEY=
TRANSFERENCIAS_BANCO_WHITELIST=bice.cl
